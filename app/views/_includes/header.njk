{{ govukHeader({
    homepageUrl: "#",
    serviceName: serviceName,
    serviceUrl: '/',
    navigation: [
      {
        href: "/account/sign-out",
        text: "Sign out"
      } if data.user,
      {
        href: "/account/sign-in",
        text: "Sign in"
      } if not data.user
    ]
  }) }}